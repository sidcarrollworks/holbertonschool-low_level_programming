#include <stdio.h>

/**
 * main - prints alphabet
 * Return: the alphabet is printed out
 */

int main(void)
{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
		putchar (c);
		putchar ('\n');

	return (0);
}
